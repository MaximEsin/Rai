import styles from "../styles/404.module.scss";

const Button = () => (
    <>
    <section>
    <button className={styles.button}>
        <h2 className={styles.buttonText}>Смотреть</h2>
        </button>
    </section>
    </>
);

export default Button;